# Screenshots

PNGs referenced from the project README. Keep them under ~500 KB each and aim
for a 16:10 or 3:2 frame so the chart and sidebar don't look cramped.

| File             | Page in the GUI                              |
|------------------|----------------------------------------------|
| `overview.png`   | Overview dashboard (live telemetry)          |
| `fans.png`       | Fans — curve editor in Curve mode            |
| `power.png`      | Power — profile segment + EC diagnostics     |
| `battery.png`    | Battery — FlexiCharger limits                |
| `keyboard.png`   | Keyboard — backlight presets + live preview  |
| `led-ring.png`   | LED Ring — Animation mode with presets       |
| `settings.png`   | Settings — themes, language, diagnostics     |

When updating: replace the file in place. README links are stable, so a
fresh PNG is enough — no markdown change required.
